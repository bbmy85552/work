# 数据库连接 URL (PostgreSQL)
# 格式: postgresql://用户名:密码@主机:端口/数据库名
# 示例:
DATABASE_URL=postgresql://username:password@localhost:5432/xuezhiai

# API 版本配置（客户端可见）
NEXT_PUBLIC_API_VERSION=v1

# 外部后端 API 代理地址（仅服务器使用，可按环境覆盖）
# 架构说明：
# - /api/* → Next.js API Routes (登录、学校数据等)
# - /backend-api/* → 外部后端服务器 (AI 生成、搜索等)
BACKEND_API_PROXY_TARGET_DEV=http://localhost:8000
BACKEND_API_PROXY_TARGET_PROD=https://api.example.com

# 图片 CDN（可为空表示使用相对路径）
NEXT_PUBLIC_IMAGE_CDN_BASE_URL=

# 简易登录账户（默认 lixiang/lixiang，可在生产环境覆盖）
AUTH_USERNAME=lixiang
AUTH_PASSWORD=lixiang
